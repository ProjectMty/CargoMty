/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cargomty.com'], // Reemplaza esto con el dominio de donde provienen tus imágenes externas
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
